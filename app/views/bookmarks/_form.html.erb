<%= simple_form_for [list, bookmark] do |f| %>
<%= f.association :movie, collection: Movie.all.order(:title), prompt: "Choose a movie" %>
  <%= f.input :comment %>
  <%= f.submit "Create", class: "btn btn-primary rounded-pill mt-3"%>
<% end %>
